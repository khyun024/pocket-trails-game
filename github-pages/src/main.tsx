import React from "react";
import ReactDOM from "react-dom/client";
import { PocketTrails } from "../../app/PocketTrails";
import "../../app/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PocketTrails />
  </React.StrictMode>,
);
